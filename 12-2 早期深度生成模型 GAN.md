## GAN 模型

- 生成器使用随机噪声生成数据，**尽可能生成逼真的数据来欺骗判别器**。
- 判别器判断数据是否真实，**尽力识别出生成器生成的假数据**。

![[Pasted image 20260718162347.png]]

#### 损失

$$\min_{G} \max_{D} V(D, G) = \mathbb{E}_{x \sim p_{\text{data}}(x)}[\log D(x)] + \mathbb{E}_{z \sim p_z(z)}[\log(1 - D(G(z)))]$$
$$
V(D,G)

\text{真实图片被识别正确}  
+  
\text{假图片被识别正确}  
$$
#### 训练

**Algorithm 1** 生成对抗网络的 mini-batch 随机梯度下降训练。判别器更新步数 $k$ 是一个超参数，实验中取 $k=1$（最便宜的选项）。

- **for 训练迭代次数 do**
  - **for $k$ 步 do**
    - 从噪声先验 $p_g(z)$ 中采样 $m$ 个噪声样本 $\{z^{(1)}, \dots, z^{(m)}\}$。
    - 从数据生成分布 $p_{\text{data}}(x)$ 中采样 $m$ 个样本 $\{x^{(1)}, \dots, x^{(m)}\}$。
    - 沿其随机梯度方向上升，更新判别器：
    $$\nabla_{\theta_d} \frac{1}{m} \sum_{i=1}^{m} \left[ \log D(x^{(i)}) + \log(1 - D(G(z^{(i)}))) \right]$$
  - **end for**
  - 从噪声先验 $p_g(z)$ 中采样 $m$ 个噪声样本 $\{z^{(1)}, \dots, z^{(m)}\}$。
  - 沿其随机梯度方向下降，更新生成器：
    $$\nabla_{\theta_g} \frac{1}{m} \sum_{i=1}^{m} \log(1 - D(G(z^{(i)})))$$
- **end for**

基于梯度的更新可以使用任何标准的基于梯度的学习规则，实验中使用了动量。

#### 损失分析

- 判别器损失分析：最优判别器用来寻找生成器生成的数据分布与真实数据分布之间的差异（JS 散度）。

#### 最优判别器推导

$$D^* = \underset{D}{\operatorname{argmax}}\, V(D, G)$$

$$= \underset{D}{\operatorname{argmax}}\, V(D)$$

$$= \underset{D}{\operatorname{argmax}} \left\{ \mathbb{E}_{x \sim p_{\text{data}}(x)} [\log D(x)] + \mathbb{E}_{z \sim p(z)} \log(1 - D(G(z))) \right\}$$

$$= \underset{D}{\operatorname{argmax}} \left\{ \mathbb{E}_{x \sim p_{\text{data}}(x)} [\log D(x)] + \mathbb{E}_{x \sim p_G(x)} \log(1 - D(x)) \right\}$$

$$= \underset{D}{\operatorname{argmax}} \left\{ \int P_{\text{data}}(x) \log D(x)\, dx + \int P_G(x) \log(1 - D(x))\, dx \right\}$$

$$= \underset{D}{\operatorname{argmax}} \left\{ \int \left[ P_{\text{data}}(x) \log D(x) + P_G(x) \log(1 - D(x)) \right] dx \right\}$$

$$= \underset{D}{\operatorname{argmax}} \left[ P_{\text{data}}(x) \log D(x) + P_G(x) \log(1 - D(x)) \right]$$

#### 求最优 $D^*$

$$F = P_{\text{data}}(x) \log D(x) + P_G(x) \log(1 - D(x))$$

$$\frac{dF}{dD(x)} = \frac{P_{\text{data}}(x)}{D(x)} - \frac{P_G(x)}{1 - D(x)}$$

令 $\dfrac{dF}{dD(x)} = 0$：

$$\frac{P_{\text{data}}(x)}{D(x)} - \frac{P_G(x)}{1 - D(x)} = 0$$

$$D^*(x) = \frac{P_{\text{data}}(x)}{P_{\text{data}}(x) + P_G(x)}$$

#### 代回原式

$$\max_D V(G, D) = V(G, D^*)$$

$$= \mathbb{E}_{x \sim p_{\text{data}}(x)} \log D^*(x) + \mathbb{E}_{x \sim p_G(x)} \log(1 - D^*(x))$$

$$= \mathbb{E}_{x \sim p_{\text{data}}(x)} \log \frac{P_{\text{data}}(x)}{P_{\text{data}}(x) + P_G(x)} + \mathbb{E}_{x \sim p_G(x)} \log \left(1 - \frac{P_{\text{data}}(x)}{P_{\text{data}}(x) + P_G(x)}\right)$$

$$= \int p_{\text{data}}(x) \log \frac{P_{\text{data}}(x)}{P_{\text{data}}(x) + P_G(x)}\, dx + \int p_G(x) \log \frac{P_G(x)}{P_{\text{data}}(x) + P_G(x)}\, dx$$

$$= \int p_{\text{data}}(x) \log \frac{P_{\text{data}}(x)}{\frac{P_{\text{data}}(x) + P_G(x)}{2}}\, dx + \int p_G(x) \log \frac{P_G(x)}{\frac{P_{\text{data}}(x) + P_G(x)}{2}}\, dx - 2\log 2$$

$$= KL\!\left(P_{\text{data}}(x) \,\Big\|\, \frac{P_{\text{data}}(x) + P_G(x)}{2}\right) + KL\!\left(P_G(x) \,\Big\|\, \frac{P_{\text{data}}(x) + P_G(x)}{2}\right) - 2\log 2$$

$$= 2\, JS\!\left(P_{\text{data}}(x) \,\|\, P_G(x)\right) - 2\log 2$$

> [!note]
> #### JS散度
> JS散度就是一种对称版 KL 散度，用来衡量两个概率分布之间的距离。GAN 证明生成器最终是在最小化真实图片分布和生成图片分布之间的 JS 距离。
> $$D_{JS}(P \| Q) = \frac{1}{2} D_{KL}\left(P \,\|\, \frac{P+Q}{2}\right) + \frac{1}{2} D_{KL}\left(Q \,\|\, \frac{P+Q}{2}\right)$$

#### 生成器损失分析

- 最优生成器是使得生成器生成的数据分布尽可能接近真实数据分布（即最小化 JS 散度）的那一个生成器。

$$\min_G \max_D V(D, G) = \min_G V(G, D^*)$$

$$= \min_G \left[ 2\, JS\!\left(P_{\text{data}}(x) \,\|\, P_G(x)\right) - 2\log 2 \right]$$

$$= \min_G JS\!\left(P_{\text{data}}(x) \,\|\, P_G(x)\right)$$
## WGAN
### GAN 的问题

- **训练不稳定（Training instability）**：在传统 GAN 中，必须仔细平衡生成器和判别器的训练程度。如果判别器训练得**太好**，梯度会消失；如果判别器训练得**太差**，梯度会不准确。
- **模式坍缩（Mode Collapse）**：生成器倾向于只生成很少种类的样本，而不是覆盖整个数据分布。

> [!note]
> #### 判别器训练得**太好**，梯度会消失：
> $$J(G) = \mathbb{E}_z \left[ \log(1 - D(G(z))) \right]$$
> 
> - $o = G(z)$：生成器的输出（生成的假图片）。
> - $r = \mathrm{logit}(o)$：判别器最后一层线性层的输出（未经过激活函数的值）。
> - $D(o) = \sigma(r) = \dfrac{1}{1 + e^{-r}}$：判别器的最终评分。
> 
> ##### 生成器的梯度
> 
> $$\nabla_{\theta_g} J(G) = \underbrace{\frac{\partial J}{\partial D(o)}}_{\text{损失对 D 输出的导数}} \cdot \underbrace{\frac{\partial D(o)}{\partial r}}_{\text{Sigmoid 的导数}} \cdot \underbrace{\frac{\partial r}{\partial o} \cdot \frac{\partial o}{\partial \theta_g}}_{\text{D 网络内部与 G 的梯度}}$$
> 
> ##### 各项具体形式
> 
> - $\sigma'(x) = \sigma(x)(1 - \sigma(x))$
> 
> - $\dfrac{\partial J}{\partial D} = \dfrac{\partial}{\partial D} \log(1 - D) = \dfrac{-1}{1 - D}$
> 
> - $\dfrac{\partial D}{\partial r} = D \cdot (1 - D)$
> ##### 带回去：
> $$\nabla_{\theta_g} J(G) = -D \cdot \frac{\partial r}{\partial o} \cdot \frac{\partial o}{\partial \theta_g}$$

#### 最优判别器下的生成器损失

$$\min_G \max_D V(D, G) = \min_G V(G, D^*) = \min_G JS\!\left(P_{\text{data}}(x) \,\|\, P_G(x)\right)$$

当真实分布和生成分布的支撑集在高维空间中都是**低维流形**时，二者重叠部分的测度为 0 的概率为 1。

> [!note]
> 中译中一下：
> 图片数据实际上只占高维空间的一小片“薄面”，生成器生成的图片也是另一片薄面，两个薄面随机情况下几乎碰不到，所以两个分布没有重叠，JS散度变成最大值，导致GAN梯度消失。

#### JS 散度推导

$$JS(P_{\text{data}} \| P_G) = \frac{1}{2} KL\!\left( P_{\text{data}} \,\Big\|\, \frac{P_{\text{data}} + P_G}{2} \right) + \frac{1}{2} KL\!\left( P_G \,\Big\|\, \frac{P_{\text{data}} + P_G}{2} \right)$$

$$= \frac{1}{2} \int P_{\text{data}}(x) \log \left( \frac{2 P_{\text{data}}(x)}{P_{\text{data}}(x) + P_G(x)} \right) dx + \frac{1}{2} \int P_G(x) \log \left( \frac{2 P_G(x)}{P_{\text{data}}(x) + P_G(x)} \right) dx = \log 2$$

> [!note]
> #### 第一项
> 
> **在 $P_{data}$ 有值的地方，$P_G(x) = 0$**，所以：$\frac{2P_{data}(x)}{P_{data}(x)+P_G(x)} = \frac{2P_{data}(x)}{P_{data}(x)} = 2$
> 
> 由于概率积分为 1：$\int P_{data}(x) dx = 1$
> 

为了解决 GAN 中的梯度消失问题，提出了改进的生成器损失。

$$\mathbb{E}_{x \sim P_g}\left[\log(1 - D(x))\right] \longrightarrow \mathbb{E}_{x \sim P_g}\left[-\log D(x)\right]$$

#### 推导改进后的损失

$$\mathbb{E}_{x \sim P_g}\left[-\log D^*(x)\right] = KL(P_g \| P_r) - \mathbb{E}_{x \sim P_g} \log\left[1 - D^*(x)\right]$$

$$= KL(P_g \| P_r) - 2\, JS(P_r \| P_g) + 2\log 2 + \mathbb{E}_{x \sim P_r}\left[\log D^*(x)\right]$$

KL 散度会**缩小**分布之间的距离，而 JS 散度会**增大**分布之间的距离，这可能导致训练不稳定；此外，KL 散度还可能导致模型**模式坍缩（collapse mode）**。
#### 两种极端情况

- 当 $P_g(x) \to 0$ 且 $P_r(x) \to 1$ 时，$P_g(x) \log \dfrac{P_g(x)}{P_r(x)} \to 0$。
- 当 $P_g(x) \to 1$ 且 $P_r(x) \to 0$ 时，$P_g(x) \log \dfrac{P_g(x)}{P_r(x)} \to +\infty$。

##### 使用 Wasserstein 距离（EM 距离）来解决 JS 距离带来的问题。

$$W(P_r, P_g) = \inf_{\gamma \sim \Pi(P_r, P_g)} \mathbb{E}_{(x,y) \sim \gamma}\left[\,\|x - y\|\,\right]$$

**Example 1（学习平行线）**。令 $Z \sim U[0,1]$ 为单位区间上的均匀分布。令 $\mathbb{P}_0$ 为 $(0, Z) \in \mathbb{R}^2$ 的分布（x 轴上为 0，y 轴上为随机变量 $Z$），均匀分布在经过原点的竖直线上。现在令 $g_\theta(z) = (\theta, z)$，其中 $\theta$ 是一个实参数。可以很容易地看出，在这种情况下：

- $W(\mathbb{P}_0, \mathbb{P}_\theta) = |\theta|$，
- $JS(\mathbb{P}_0, \mathbb{P}_\theta) = \begin{cases} \log 2 & \text{if } \theta \neq 0, \\ 0 & \text{if } \theta = 0, \end{cases}$
- $KL(\mathbb{P}_0 \| \mathbb{P}_\theta) = KL(\mathbb{P}_0 \| \mathbb{P}_\theta) = \begin{cases} +\infty & \text{if } \theta \neq 0, \\ 0 & \text{if } \theta = 0, \end{cases}$
![[Pasted image 20260718222348.png|351]]

> [!note]
> EM距离（Earth Mover's Distance）就是把一个概率分布“搬成”另一个概率分布所需的最小运输成本，比 KL/JS 更适合比较没有重叠的分布。对于离散情况可以使用二分图匹配的方式，EM距离就是在一个二分图上寻找最小代价的概率流匹配。

#### 损失：
Wasserstein 距离中的 $\inf_{\gamma \sim \Pi(P_r, P_g)}$ 无法求解，经过推导得到下式：

$$W(P_r, P_g) = \frac{1}{K} \sup_{\|f\|_L \leq K} \mathbb{E}_{x \sim P_r}[f(x)] - \mathbb{E}_{x \sim P_g}[f(x)]$$

$$|f(x_1) - f(x_2)| \leq K |x_1 - x_2| \quad \text{Lipschitz 连续}$$

$$K \cdot W(P_r, P_g) \approx \max_{w: |f_w|_L \leq K} \mathbb{E}_{x \sim P_r}[f_w(x)] - \mathbb{E}_{x \sim P_g}[f_w(x)]$$

用判别器（神经网络）去拟合函数 $f_w(x)$。
通过将判别器的参数限制在 $[-c, c]$ 范围内，使得必然存在某个 $K$ 满足 $|f_w|_L \leq K$。

- 判别器损失：$$\mathbb{E}_{x \sim P_g}[f_w(x)] - \mathbb{E}_{x \sim P_r}[f_w(x)]$$
- 生成器损失：$$-\mathbb{E}_{x \sim P_g}[f_w(x)]$$
> [!note]
> Wasserstein 原本是在找“最优搬运路线”，太难算，所以换成找一个平滑函数 fff，让真实数据平均得分比生成数据高多少。WGAN 用神经网络模拟这个 fff，这个网络不负责判断真假，而负责给样本打分。这个分数差就是两个分布的距离。
> 原始 Wasserstein 是一个带线性约束的优化问题，所以通过拉格朗日对偶，把“优化搬运方案 γ”转换成“优化一个满足 Lipschitz 约束的函数 f”。

**Algorithm 1** WGAN，我们提出的算法。论文中所有实验使用的默认值为 $\alpha = 0.00005$，$c = 0.01$，$m = 64$，$n_{\text{critic}} = 5$。

- **Require：** $\alpha$，学习率。$c$，裁剪参数。$m$，batch size。$n_{\text{critic}}$，每次生成器迭代中 critic 的迭代次数。
- **Require：** $w_0$，初始 critic 参数。$\theta_0$，初始生成器参数。

1. **while** $\theta$ **has not converged do**
2.   **for** $t = 0, \dots, n_{\text{critic}}$ **do**
3.     从真实数据中采样一个 batch $\{x^{(i)}\}_{i=1}^{m} \sim \mathbb{P}_r$。
4.     从先验中采样一个 batch $\{z^{(i)}\}_{i=1}^{m} \sim p(z)$。
5.     $g_w \leftarrow \nabla_w \left[ \dfrac{1}{m} \sum_{i=1}^{m} f_w(x^{(i)}) - \dfrac{1}{m} \sum_{i=1}^{m} f_w(g_\theta(z^{(i)})) \right]$
6.     $w \leftarrow w + \alpha \cdot \mathrm{RMSProp}(w, g_w)$
7.     $w \leftarrow \mathrm{clip}(w, -c, c)$
8.   **end for**
9.   从先验中采样一个 batch $\{z^{(i)}\}_{i=1}^{m} \sim p(z)$。
10.  $g_\theta \leftarrow -\nabla_\theta \dfrac{1}{m} \sum_{i=1}^{m} f_w(g_\theta(z^{(i)}))$
11.  $\theta \leftarrow \theta - \alpha \cdot \mathrm{RMSProp}(\theta, g_\theta)$
12. **end while**
