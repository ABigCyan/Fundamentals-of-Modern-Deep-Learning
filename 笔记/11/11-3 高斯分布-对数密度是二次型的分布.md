## 实际中我们会选择让学习**可计算**的分布。

### 高斯分布很特别，因为：

- **易于采样：** 只需要一个基础噪声源 $\epsilon \sim \mathcal{N}(0, I)$。
- 在线性 / 仿射变换下封闭：神经网络本身就是仿射变换 + 非线性激活的堆叠。
- **<font color="#00b050">信息量有解析形式：</font>** <font color="#00b050">KL、熵、对数似然等项常常有闭式解。</font>
- **原理上的"默认"选择：** 在给定均值 + 协方差的约束下，它是最大熵分布。

**结论：高斯分布使 ELBO 的各项都可计算，并且让梯度的方差较低。**

**核心洞见：高斯 = 对数密度是二次函数的分布。** 下面列出的所有性质都来源于二次函数的代数性质。

### 一、高斯的两种表示

#### 矩形式（moment form）：$x \sim \mathcal{N}(\mu, \Sigma)$，$\Sigma \succ 0$。

$$\log p(x) = -\frac{1}{2}\Big((x - \mu)^\top \Sigma^{-1}(x - \mu) + \log|\Sigma| + d\log(2\pi)\Big).$$

#### 信息（典范）形式（canonical form）：精度 $\Lambda = \Sigma^{-1}$。

$$p(x) \propto \exp\!\left(-\tfrac{1}{2} x^\top \Lambda x + b^\top x\right), \qquad \mu = \Lambda^{-1} b,\ \Sigma = \Lambda^{-1}.$$

**为什么有两种形式：** 一个二次函数 $f(x) = -\tfrac{1}{2} x^\top \Lambda x + b^\top x + c$ 既可以用系数 $(\Lambda, b)$ 描述，也可以用它的极值点 $\mu = \Lambda^{-1} b$ 和曲率 $\Lambda$ 来描述。同一个二次型，两种看法。

**为什么重要：** 典范形式让"组合证据"这件事变成了纯粹的代数运算（二次型相加 $\Rightarrow$ 精度相加）。

## 二、在线性 / 仿射变换下封闭
### 性质：若 $x \sim \mathcal{N}(\mu, \Sigma)$，则对任意矩阵 $A$ 和向量 $c$：

$$y = Ax + c \quad \Longrightarrow \quad y \sim \mathcal{N}(A\mu + c,\ A\Sigma A^\top).$$

> [!note]
> **为什么重要：** 这让**重参数化技巧**成为可能：采样 $z = \mu + L\epsilon$，其中 $\epsilon \sim \mathcal{N}(0, I)$，这样梯度就能穿过 $\mu$ 和 $L$ 反向传播。
> 
> **为什么成立：** 把 $x = A^{-1}(y - c)$ 代入 $(x - \mu)^\top \Sigma^{-1}(x - \mu)$，会得到一个关于 $y$ 的二次型。**二次型上的线性变量替换仍然是二次型。**
> 

## 三、边缘分布的封闭性
### 性质：将 $x$ 分块为

$$x = \begin{bmatrix} x_1 \\ x_2 \end{bmatrix} \sim \mathcal{N}\!\left( \begin{bmatrix} \mu_1 \\ \mu_2 \end{bmatrix},\ \begin{bmatrix} \Sigma_{11} & \Sigma_{12} \\ \Sigma_{21} & \Sigma_{22} \end{bmatrix} \right).$$

则每个分块的边缘分布仍是高斯：

$$x_1 \sim \mathcal{N}(\mu_1, \Sigma_{11}), \qquad x_2 \sim \mathcal{N}(\mu_2, \Sigma_{22}).$$
> [!note]
> **边缘分布计算过程：**
> 用 2D 例子：
$$x=\begin{bmatrix}x_1\\x_2\end{bmatrix}\sim\mathcal{N}\!\left(\begin{bmatrix}0\\0\end{bmatrix},\ \begin{bmatrix}1&\rho\\\rho&1\end{bmatrix}\right)$$
> $$\underbrace{p(x_1,x_2)}_{\text{联合}}=\frac{1}{\text{常数}}\cdot\underbrace{e^{-\frac{1}{2}x^T\Sigma^{-1}x}}_{\text{二次型}}\xrightarrow[\text{积分掉 }x_2]{\text{对 }x_2\text{ 配方}}\frac{1}{\text{常数}}\cdot\underbrace{e^{-\frac{x_1^2}{2}}}_{p(x_1)\text{ 的指数}}\cdot\underbrace{\sqrt{2\pi(1-\rho^2)}}_{x_2\text{ 的归一化}}=\mathcal{N}(0,1)$$
> 这里第二项对 $x_2$ 是个高斯，**均值 $\rho x_1$，方差 $1-\rho^2$**，归一化直接给出：
> 
> $$\int_{-\infty}^{\infty}\exp\!\left(-\frac{(x_2-\rho x_1)^2}{2(1-\rho^2)}\right)dx_2=\sqrt{2\pi(1-\rho^2)}$$
> 


**为什么重要：** 把潜变量积分掉是**精确且廉价**的——根本不需要 Monte Carlo。

**为什么成立：** 联合对数密度是关于 $(x_1, x_2)$ 的二次型。把 $x_2$ 积掉，就是对 $x_2$ 的二次项做高斯积分，结果仍然是关于 $x_1$ 的二次型。**二次型的高斯积分 $\Rightarrow$ 关于剩余变量的二次型。**

## 四、条件分布的封闭性
### 性质：在同样的分块下，

$$x_1 \mid x_2 \sim \mathcal{N}\!\left(\mu_1 + \Sigma_{12}\Sigma_{22}^{-1}(x_2 - \mu_2),\ \Sigma_{11} - \Sigma_{12}\Sigma_{22}^{-1}\Sigma_{21}\right).$$

**为什么重要：** 线性-高斯模型有**闭式后验**（卡尔曼滤波、贝叶斯线性回归）。

**为什么成立：** $\log p(x_1 \mid x_2) = \log p(x_1, x_2) - \log p(x_2)$。两项都是二次型，相减把只含 $x_2$ 的项消掉，剩下的就是关于 $x_1$ 的二次型（系数依赖于 $x_2$）。**两个不同变量的二次型相减 $\Rightarrow$ 仍是二次型。**

> [!note]
> **依旧使用刚才的2d高斯的例子：**
> 
> $$\log p(x_1|x_2)=\underbrace{-\frac{(x_1-\rho x_2)^2}{2(1-\rho^2)}}_{只剩这个}+\underbrace{\frac{x_2^2}{2}-\frac{x_2^2}{2}}_{=0}+\text{const}$$
> $$\boxed{\log p(x_1|x_2)=-\frac{(x_1-\rho x_2)^2}{2(1-\rho^2)}+\text{const}}$$
> 这就是 $\mathcal{N}(\rho x_2,\ 1-\rho^2)$。

## 五、独立高斯的可加性

**性质：** 若 $x \sim \mathcal{N}(\mu_x, \Sigma_x)$，$y \sim \mathcal{N}(\mu_y, \Sigma_y)$，且 $x \perp y$，则

$$x + y \sim \mathcal{N}(\mu_x + \mu_y,\ \Sigma_x + \Sigma_y).$$

**为什么重要：** 噪声累积是解析可求的；例如扩散模型（diffusion models）能够在每一步精确地追踪方差。

**为什么成立：** 独立性意味着 $\log p(x, y) = \log p(x) + \log p(y)$，两个二次型相加 $\Rightarrow$ 联合是高斯。然后 $z = x + y$ 是线性映射，保持高斯性。独立性把交叉项 $\mathrm{Cov}(x, y)$ 消掉，所以方差直接相加。

**核心：二次型求和 + 线性映射 + 零交叉协方差。**

## 六、高斯之积 = 精度相加

**性质：** 在典范形式（canonical form）下，

$$p_1(x)\, p_2(x) \;\propto\; \exp\!\left(-\tfrac{1}{2} x^\top (\Lambda_1 + \Lambda_2) x + (b_1 + b_2)^\top x\right).$$

**结果：** $\Lambda = \Lambda_1 + \Lambda_2$，$b = b_1 + b_2$，$\mu = \Lambda^{-1} b$。

**为什么重要：** 贝叶斯更新和信息传递都化归为**加法**——纯粹的线性代数。

**为什么成立：** $\exp(f) \cdot \exp(g) = \exp(f + g)$。二次型 + 二次型 = 二次型，系数直接相加。

**核心：密度上的乘法 $\Leftrightarrow$ 对数密度上的加法 $\Leftrightarrow$ 二次型系数的加法。**

## 七、高斯之间的 KL 散度（VAE 中的 KL 项）

**性质：** 对 $q = \mathcal{N}(\mu_0, \Sigma_0)$ 和 $p = \mathcal{N}(\mu_1, \Sigma_1)$：

$$\mathrm{KL}(q \,\|\, p) = \frac{1}{2}\left(\mathrm{tr}(\Sigma_1^{-1}\Sigma_0) + (\mu_1 - \mu_0)^\top \Sigma_1^{-1}(\mu_1 - \mu_0) - d + \log \frac{|\Sigma_1|}{|\Sigma_0|}\right).$$

**VAE 的特殊情形**（$p = \mathcal{N}(0, I)$，$q = \mathcal{N}(\mu, \mathrm{diag}(\sigma^2))$）：

$$\mathrm{KL}(q \,\|\, p) = \frac{1}{2} \sum_{i=1}^{d}\left(\mu_i^2 + \sigma_i^2 - \log \sigma_i^2 - 1\right).$$

**为什么重要：** KL 有闭式解 $\Rightarrow$ 正则项**不需要 Monte Carlo 估计**；梯度方差也更低。

**为什么成立：** $\mathrm{KL} = \mathbb{E}_q[\log q - \log p]$。两个对数密度都是关于 $x$ 的二次型；二次型在某个高斯分布下的期望，只取决于该高斯的前两阶矩——而这两阶矩正是我们已知的参数。

**核心：高斯下的二次型期望 = 仅依赖于前两阶矩的函数。**

> [!note]
> **KL 散度（相对熵）**
> 两个分布 $p$、$q$ 之间的距离（**不是真的距离，不对称**）：
> 
> $$D_{\mathrm{KL}}(p\|q)=\mathbb{E}_{x\sim p}\!\left[\log\frac{p(x)}{q(x)}\right]=\int p(x)\log\frac{p(x)}{q(x)}\,dx$$
> 
> 离散版：
> 
> $$D_{\mathrm{KL}}(p\|q)=\sum_x p(x)\log\frac{p(x)}{q(x)}$$

## 八、高斯的熵

**性质：** 若 $x \sim \mathcal{N}(\mu, \Sigma)$，则

$$\mathcal{H}(x) = \frac{1}{2} \log \big((2\pi e)^d |\Sigma|\big).$$

**对角 $\Sigma$ 的情况：** $\mathcal{H}(x) = \dfrac{1}{2} \displaystyle\sum_{i=1}^{d} \log(2\pi e\, \sigma_i^2)$。

**为什么重要：** ELBO 中的 $-\mathbb{E}_q[\log q]$ 项是**精确可求**的；再结合 KL 的解析形式，ELBO 的很多部分根本不需要采样。

**为什么成立：** $\mathcal{H} = \mathbb{E}[-\log p]$；$-\log p$ 是二次型。它在 $p$ 下的期望要用到 $\mathbb{E}[(x-\mu)(x-\mu)^\top] = \Sigma$，从而得到 $\mathrm{tr}(\Sigma^{-1}\Sigma) = d$ 和 log-det 项。

**和 KL 是同一个原因：二次型的期望具有闭式解。**

> [!note]
> **熵（entropy）= 信息量的期望**
> 
> $$H(p)=\mathbb{E}_{x\sim p}[I(x)]=\mathbb{E}_{x\sim p}[-\log p(x)]=-\sum_x p(x)\log p(x)$$
> 
> 连续版：
> 
> $$H(p)=-\int p(x)\log p(x)\,dx$$
> 
> 直观：**分布越「平」/越不确定，熵越大**。
> - 均匀分布熵最大
> - 确定性分布（一个点）熵 = 0

## 九、最大熵性质

**性质：** 在所有给定均值 $\mu$ 和协方差 $\Sigma$ 的分布中，**高斯分布具有最大的微分熵**。

**为什么重要：** 高斯只编码了**一阶和二阶矩**——除了这些之外没有引入任何额外假设。所以它是一个原理上"最少承诺"的先验。

**为什么成立：** 约束 $\mathbb{E}[x] = \mu$ 和 $\mathbb{E}[(x-\mu)(x-\mu)^\top] = \Sigma$，然后用 Lagrange 乘子最大化熵。乘子会在 $\log p(x)$ 中引入线性项和二次项 $\Rightarrow$ 解出来就是高斯。

> [!note]
> [[11-4最大熵 → 高斯（完整推导）]]

**推广：$k$ 阶矩约束 $\Rightarrow$ 对数密度是 $k$ 次多项式。只约束一阶矩就是指数分布，约束一阶矩和二阶矩就是高斯分布**